node_modules/
dist/

# Documentación de operación y salidas de auditoría: se mantienen fuera del
# repositorio a propósito. Siguen existiendo en local; ignorarlas evita que
# vuelvan a entrar sin decidirlo.
docs/
audit-output/

# Copias de seguridad (ISSUE-015). Un dump del piloto contiene datos de
# clientes, ventas y hashes de contraseña: no entra en Git bajo ningún nombre.
ops/backups/
*.dump
*.dump.parcial
*.dump.sha256
*.sql.gz

# Salidas del E2E (Playwright): trazas, capturas e informes se generan en cada
# ejecución y solo se publican como artefacto de CI cuando el job falla.
test-results/
playwright-report/
blob-report/
/playwright/.cache/
